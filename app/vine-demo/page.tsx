import { VineExperience } from "@/components/VineExperience";

export const metadata = {
  title: "Growing vine · The Hub",
  description: "Leave a word. Every word becomes a leaf on a shared vine.",
};

export default function VineDemoPage() {
  return <VineExperience />;
}
