export type HubCard = {
  id: string;
  word: string;
  seed: string;
  created_at: string;
};

export type VisualTheme =
  | "curious"
  | "calm"
  | "bold"
  | "creative"
  | "analytical"
  | "neutral";
